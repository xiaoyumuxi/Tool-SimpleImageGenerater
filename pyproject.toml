[project]
name = "tool"
version = "0.1.0"
description = "Generate images with an OpenAI-compatible relay after auto-detecting an image-2 model."
requires-python = ">=3.12"
dependencies = [
    "curl-cffi>=0.7.4",
]
